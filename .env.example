NODE_ENV=development
DATABASE_URL=mysql://user:password@127.0.0.1:3306/joyful_memorial
JWT_SECRET=replace-with-a-long-random-secret
UPLOAD_DIR=./uploads
OWNER_OPEN_ID=
OAUTH_SERVER_URL=
VITE_APP_ID=joyful-memorial

# Optional: when set, `npm run db:seed` also creates a protected family room
# for the sample memorial. Leave unset if you only want the public sample.
SAMPLE_FAMILY_ROOM_PASSWORD=
